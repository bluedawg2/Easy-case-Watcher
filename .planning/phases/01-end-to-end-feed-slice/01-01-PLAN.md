---
phase: 01-end-to-end-feed-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - docker-compose.yml
  - alembic.ini
  - .env.example
  - .gitignore
  - src/brm/__init__.py
  - src/brm/config.py
  - src/brm/db.py
  - src/brm/lifecycle.py
  - src/brm/models/__init__.py
  - src/brm/models/source.py
  - src/brm/models/snapshot.py
  - src/brm/models/change.py
  - src/brm/seed.py
  - alembic/env.py
  - alembic/versions/0001_initial_schema.py
  - tests/conftest.py
  - tests/test_models.py
  - tests/test_lifecycle.py
autonomous: false
requirements: [SRC-01, SRC-02, EFF-01, EFF-02]
user_setup:
  - service: docker
    why: "Local PostgreSQL 16 container — Postgres is not installed natively"
    dashboard_config:
      - task: "Install Docker Desktop and ensure the daemon is running"
        location: "https://www.docker.com/products/docker-desktop/"

must_haves:
  truths:
    - "A developer can run `uv run pytest` and the suite executes against a real Postgres 16 database"
    - "The schema holds a Source record with jurisdiction, layer, feed URL, ingestion method, cadence, last-checked and last-changed state, and health status"
    - "The schema holds a Change record with detected date and effective date as separate columns and a status field; the effective_date column is nullable so it can stay empty until a reviewer enters it during review (D-12)"
    - "An illegal lifecycle status transition is rejected by the transition guard"
    - "Running the seed script inserts exactly one national FRBP source row"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv-managed backend project with pinned dependencies and pytest config"
      contains: "tool.pytest.ini_options"
    - path: "src/brm/models/source.py"
      provides: "Source ORM model with full registry columns"
      contains: "class Source"
    - path: "src/brm/models/change.py"
      provides: "Change ORM model with detected_at, effective_date, status columns"
      contains: "class Change"
    - path: "src/brm/models/snapshot.py"
      provides: "Append-only Snapshot ORM model"
      contains: "class Snapshot"
    - path: "src/brm/lifecycle.py"
      provides: "Status constants and allowed-transition guard"
      contains: "ALLOWED_TRANSITIONS"
    - path: "alembic/versions/0001_initial_schema.py"
      provides: "Initial migration creating source, snapshot, change tables"
      contains: "create_table"
  key_links:
    - from: "src/brm/db.py"
      to: "PostgreSQL 16"
      via: "create_async_engine with postgresql+psycopg driver"
      pattern: "postgresql\\+psycopg"
    - from: "src/brm/models/change.py"
      to: "src/brm/lifecycle.py"
      via: "status CHECK constraint references the allowed status values"
      pattern: "CheckConstraint"
---

<objective>
Stand up the Walking Skeleton's foundation: a uv-managed Python 3.12 / FastAPI backend project, a local Postgres 16 via Docker, and the three core domain models (Source, Snapshot, Change) with Alembic migrations. This is the first slice — after this plan a developer can run a test suite against a real database and seed the one FRBP source the whole pipeline runs on.

Purpose: Every subsequent slice (fetch, detect, summarize, review/API) reads and writes these tables. The lifecycle state machine and the tri-date Change model are established here so no later plan re-litigates the schema.
Output: Runnable backend project, Postgres container, three migrated tables, lifecycle guard, seed script, passing model + lifecycle tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-end-to-end-feed-slice/01-CONTEXT.md
@.planning/phases/01-end-to-end-feed-slice/01-RESEARCH.md
@.planning/phases/01-end-to-end-feed-slice/SKELETON.md

<interfaces>
<!-- These contracts are CREATED by this plan. Downstream plans (01-02..01-05) import them. -->
<!-- FetchOutcome / FetchResult / SourceAdapter live in plan 01-02 (ingest/), not here. -->

src/brm/lifecycle.py exports:
  STATUS_DETECTED = "detected"      # diff produced, Change created
  STATUS_PROCESSED = "processed"    # AI summary attached
  STATUS_IN_REVIEW = "in_review"    # surfaced in review queue
  STATUS_VERIFIED = "verified"      # reviewer approved
  STATUS_REJECTED = "rejected"      # reviewer marked noise
  ALL_STATUSES: list[str]           # the five values above
  ALLOWED_TRANSITIONS: dict[str, set[str]]
  class IllegalTransitionError(Exception)
  def assert_transition(current: str, target: str) -> None  # raises IllegalTransitionError

src/brm/db.py exports:
  engine                            # async engine
  SessionLocal                      # async_sessionmaker
  async def get_session() -> AsyncSession   # FastAPI dependency, yields one session per request
  class Base(DeclarativeBase)        # ORM declarative base

src/brm/models exports: Source, Snapshot, Change
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold the uv backend project, Postgres container, and async DB layer</name>
  <files>pyproject.toml, docker-compose.yml, .env.example, .gitignore, alembic.ini, alembic/env.py, src/brm/__init__.py, src/brm/config.py, src/brm/db.py, tests/conftest.py</files>
  <read_first>
    - CLAUDE.md (locked stack, versions, "What NOT to Use" table — psycopg 3 not psycopg2, no Redis, no agent framework)
    - .planning/phases/01-end-to-end-feed-slice/01-RESEARCH.md (§ "Standard Stack" install commands; § "Code Examples → Async DB session dependency"; § "Common Pitfalls" Pitfall 6)
    - .planning/phases/01-end-to-end-feed-slice/SKELETON.md (directory layout, architectural decisions)
  </read_first>
  <behavior>
    - Test: `tests/conftest.py` provides an async session fixture that connects to the test Postgres and rolls back per test.
    - Test: importing `brm.db` exposes `engine`, `SessionLocal`, `get_session`, `Base` without raising.
    - Test: `brm.config.Settings` loads `database_url` and `anthropic_api_key` from environment / `.env`.
  </behavior>
  <action>
    Run `uv init` at repo root `C:\data\cc` (the repo currently contains only `.planning/`, `.claude/`, `CLAUDE.md`). Add backend dependencies with the exact pinned versions from RESEARCH § Standard Stack: `fastapi==0.136.1`, `uvicorn[standard]==0.47.0`, `sqlalchemy==2.0.49`, `alembic==1.18.4`, `psycopg[binary]==3.3.4`, `pydantic==2.13.4`, `pydantic-settings`, `anthropic==0.103.1`, `feedparser==6.0.12`, `httpx==0.28.1`. Add dev dependencies: `ruff`, `pytest`, `pytest-asyncio`, `respx`.
    Configure `pyproject.toml [tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`. Configure `[tool.ruff]` with line-length 100.
    Create `docker-compose.yml` with one service `db`: image `postgres:16`, env `POSTGRES_USER=brm`, `POSTGRES_PASSWORD=brm`, `POSTGRES_DB=brm`, port `5432:5432`, a named volume for data. Document that tests use a separate database `brm_test` (create it via `POSTGRES_DB` override or a second compose service — choose one and document in SKELETON.md notes).
    Create `src/brm/config.py` with a `pydantic-settings` `Settings` class exposing `database_url: str` and `anthropic_api_key: str`, reading from a git-ignored `.env`. Provide `.env.example` with placeholder values. Add `.env`, `__pycache__`, `.venv`, `*.egg-info` to `.gitignore`.
    Create `src/brm/db.py` per RESEARCH "Async DB session dependency": `create_async_engine` with a `postgresql+psycopg://` URL from `Settings.database_url`, an `async_sessionmaker` named `SessionLocal` with `expire_on_commit=False`, an async `get_session()` dependency yielding one `AsyncSession`, and a `Base(DeclarativeBase)` class. Do NOT use psycopg2 and do NOT create a module-level shared session (RESEARCH Pitfall 6).
    Create `tests/conftest.py` with an async fixture that wraps each test in a transaction and rolls back, plus a `respx` mock fixture stub (used by plan 01-02). Configure it to point at the `brm_test` database.
    Run `alembic init alembic`, then edit `alembic/env.py` to import `Base.metadata` from `brm.db`, read the URL from `brm.config.Settings`, and run migrations in async mode.
  </action>
  <verify>
    <automated>uv run python -c "import brm.db, brm.config; print('ok')" && uv run ruff check src/</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` exists with all 10 backend deps at the pinned versions and a `[tool.pytest.ini_options]` table with `asyncio_mode = "auto"`.
    - `docker-compose.yml` defines a `postgres:16` service exposing port 5432.
    - `uv run python -c "import brm.db"` exits 0 and `brm.db` exposes `engine`, `SessionLocal`, `get_session`, `Base`.
    - `src/brm/db.py` connection string contains `postgresql+psycopg` and does NOT contain `psycopg2`.
    - `.gitignore` contains `.env`; `.env.example` exists with placeholder `DATABASE_URL` and `ANTHROPIC_API_KEY`.
    - `uv run ruff check src/` exits 0.
  </acceptance_criteria>
  <done>Backend project initialized, Docker Postgres defined, async DB layer importable and lint-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Source, Snapshot, Change models, the lifecycle guard, and the initial migration</name>
  <files>src/brm/models/__init__.py, src/brm/models/source.py, src/brm/models/snapshot.py, src/brm/models/change.py, src/brm/lifecycle.py, alembic/versions/0001_initial_schema.py, tests/test_models.py, tests/test_lifecycle.py</files>
  <read_first>
    - src/brm/db.py (the `Base` declarative class — created in Task 1)
    - .planning/phases/01-end-to-end-feed-slice/01-RESEARCH.md (§ "Architecture Patterns → Pattern 3 Append-Only Snapshot Store", "Pattern 5 Lifecycle State Machine"; § "Common Pitfalls" Pitfall 5 — enum vs VARCHAR+CHECK; § "Open Questions" 1)
    - .planning/REQUIREMENTS.md (SRC-01 column list, EFF-01, EFF-02)
    - .planning/phases/01-end-to-end-feed-slice/01-CONTEXT.md (D-12 — effective date nullable, reviewer-entered)
  </read_first>
  <behavior>
    - Test (test_models.py::test_source_columns): a `Source` instance accepts and persists `jurisdiction`, `layer`, `feed_url`, `ingestion_method`, `adapter_ref`, `polling_cadence`, `last_checked_at`, `last_changed_at`, `health_status`, `last_etag`, `last_modified_http`, `last_content_hash`, `tenant_id` (nullable).
    - Test (test_models.py): a `Change` instance has distinct `detected_at` and `effective_date` columns; `effective_date` is nullable and `detected_at` is not.
    - Test (test_models.py): a `Snapshot` row stores `source_id`, `content`, `content_hash`, `version`, `fetched_at`.
    - Test (test_lifecycle.py): `assert_transition("detected", "processed")` succeeds; `assert_transition("detected", "verified")` raises `IllegalTransitionError`; `assert_transition("verified", "detected")` raises.
  </behavior>
  <action>
    Create `src/brm/lifecycle.py` with the five status string constants `STATUS_DETECTED="detected"`, `STATUS_PROCESSED="processed"`, `STATUS_IN_REVIEW="in_review"`, `STATUS_VERIFIED="verified"`, `STATUS_REJECTED="rejected"`; `ALL_STATUSES` listing all five; and `ALLOWED_TRANSITIONS` mapping: `detected → {processed}`, `processed → {in_review}`, `in_review → {verified, rejected}`, `verified → {}`, `rejected → {}`. Add `IllegalTransitionError(Exception)` and `assert_transition(current, target)` that raises it when `target not in ALLOWED_TRANSITIONS[current]`. Do NOT add `classified`, `pending-effective`, `active`, or `superseded` — those are Phases 5/6 (RESEARCH anti-patterns).
    Create `src/brm/models/source.py` with `class Source(Base)`, table `source`: columns `id` (PK), `jurisdiction` (str), `layer` (str — e.g. "FRBP"), `feed_url` (str), `ingestion_method` (str — e.g. "rss"), `adapter_ref` (str — e.g. "rss"), `polling_cadence` (str), `last_checked_at` (datetime nullable), `last_changed_at` (datetime nullable), `health_status` (str, default "unknown"), `last_etag` (str nullable), `last_modified_http` (str nullable), `last_content_hash` (str nullable), `tenant_id` (str nullable — multi-tenancy seam per CLAUDE.md), `created_at`. This satisfies SRC-01.
    Create `src/brm/models/snapshot.py` with `class Snapshot(Base)`, table `snapshot`: `id` (PK), `source_id` (FK → source.id), `content` (Text — normalized content), `content_hash` (str), `version` (int — monotonic per source), `fetched_at` (datetime), `created_at`. Append-only — no update/delete in normal operation (RESEARCH Pattern 3).
    Create `src/brm/models/change.py` with `class Change(Base)`, table `change`: `id` (PK), `source_id` (FK), `prior_snapshot_id` (FK → snapshot.id, nullable), `current_snapshot_id` (FK → snapshot.id), `status` (str, default `STATUS_DETECTED`), `detected_at` (datetime, NOT NULL), `effective_date` (Date, NULLABLE — reviewer-entered per D-12), `diff_text` (Text nullable), `summary` (JSONB nullable — filled in plan 01-04), `not_legal_advice_label` (str nullable — server constant, plan 01-04), `reviewer_name` (str nullable — plan 01-05), `model_id` (str nullable — AI reproducibility), `tenant_id` (str nullable), `created_at`, `updated_at`. Add a `CheckConstraint` restricting `status` to `ALL_STATUSES` values (VARCHAR + CHECK per RESEARCH Open Question 1 / Pitfall 5 — NOT a native PG enum, so the taxonomy can extend in Phases 5/6). This satisfies EFF-01 (separate date fields) and EFF-02 (status field).
    Export all three models from `src/brm/models/__init__.py`.
    Generate the initial migration: `uv run alembic revision --autogenerate -m "initial schema"`, rename the produced file to `0001_initial_schema.py`. Inspect the generated migration — confirm it creates `source`, `snapshot`, `change` with the CHECK constraint and FKs; hand-correct if autogenerate missed the CHECK constraint.
    Write `tests/test_models.py` and `tests/test_lifecycle.py` implementing the behaviors above.
  </action>
  <verify>
    <automated>docker compose up -d db && uv run alembic upgrade head && uv run pytest tests/test_models.py tests/test_lifecycle.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` creates `source`, `snapshot`, `change` tables without error.
    - `tests/test_models.py::test_source_columns` passes — Source has all 14 SRC-01 registry columns including nullable `tenant_id`.
    - `Change` model has `detected_at` (NOT NULL) and `effective_date` (Date, NULLABLE) as distinct columns (EFF-01; nullable-until-reviewer-entered per D-12).
    - `change` table has a CHECK constraint on `status`; `status` column type is VARCHAR, not a native PG ENUM.
    - `tests/test_lifecycle.py` passes: `detected→processed` allowed, `detected→verified` and `verified→detected` raise `IllegalTransitionError`.
    - `lifecycle.py` does NOT define `classified`, `pending_effective`, `active`, or `superseded`.
  </acceptance_criteria>
  <done>Three tables migrated, lifecycle guard enforces the Phase 1 subset, model + lifecycle tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The backend project scaffold, a local Postgres 16 container, the three core domain tables (source, snapshot, change), the lifecycle transition guard, and a passing model/lifecycle test suite. The seed of the FRBP source row happens in plan 01-02.</what-built>
  <how-to-verify>
    1. Ensure Docker Desktop is running. From `C:\data\cc` run `docker compose up -d db` — confirm a `postgres:16` container starts.
    2. Run `uv run alembic upgrade head` — confirm it reports applying revision `0001`.
    3. Run `uv run pytest -x -q` — confirm `test_models.py` and `test_lifecycle.py` pass.
    4. Inspect the generated `alembic/versions/0001_initial_schema.py` — confirm the `change` table has a CHECK constraint listing exactly the five statuses and that `effective_date` is nullable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → Postgres container | DB credentials cross here; local-only in Phase 1 |
| process → environment | `ANTHROPIC_API_KEY` and DB credentials loaded from env/`.env` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | DB credentials / `ANTHROPIC_API_KEY` in source or image | mitigate | Secrets loaded via `pydantic-settings` from a git-ignored `.env`; `.env` added to `.gitignore`; `.env.example` holds placeholders only |
| T-01-02 | Tampering | SQL injection via future query layers | mitigate | SQLAlchemy 2.0 ORM with parameterized queries established as the only DB access pattern; no string-formatted SQL anywhere |
| T-01-SC | Tampering | uv/pip package installs | mitigate | All packages drawn from CLAUDE.md LOCKED stack and verified in RESEARCH § Package Legitimacy Audit (no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages); reviewer inspects the generated `uv.lock` |
</threat_model>

<verification>
- `docker compose up -d db` starts Postgres 16.
- `uv run alembic upgrade head` applies the initial migration cleanly.
- `uv run pytest tests/test_models.py tests/test_lifecycle.py -x -q` is green.
- `uv run ruff check src/` is clean.
</verification>

<success_criteria>
- Backend project is uv-managed with all pinned dependencies installed.
- Three tables (source, snapshot, change) exist with SRC-01 columns, separate detected/effective date fields (EFF-01), and a status CHECK constraint (EFF-02).
- The `change.effective_date` column is nullable so it stays empty until a reviewer enters it during review (D-12).
- Lifecycle guard rejects illegal transitions.
- Model and lifecycle tests pass against a real Postgres 16 database.
</success_criteria>

<output>
Create `.planning/phases/01-end-to-end-feed-slice/01-01-SUMMARY.md` when done.
</output>
</output>
