---
phase: 03-pdf-ingestion-district-coverage
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/brm/seed.py
  - tests/test_source_onboarding.py
  - tests/fixtures/pdf/.gitkeep
autonomous: true
requirements: [SRC-04]
user_setup: []

must_haves:
  truths:
    - "The source registry covers state exemption rules for the launch states containing the v1 district tranche (Oregon, California, Texas)"
    - "State exemption sources reuse the existing PDF adapter; HTML-statute rows are seeded as placeholders pending the Phase-2 HTML adapter — no new adapter code is written in this phase"
    - "A genuinely tabular exemption source (the California EJ-156 PDF) is flagged tabular=true so pdfplumber extracts it"
    - "A change to an exemption statute's text — including a dollar amount inside it — is detected as a text diff"
    - "State exemption sources onboard as config-only registry rows (SRC-06)"
  artifacts:
    - path: "src/brm/seed.py"
      provides: "launch-state exemption Source rows for OR, CA, TX"
      contains: "state_exemptions"
    - path: "tests/test_source_onboarding.py"
      provides: "state-exemption onboarding + tabular-PDF extraction proof"
      contains: "tabular"
  key_links:
    - from: "src/brm/seed.py"
      to: "src/brm/models/source.py"
      via: "state exemption Source rows set layer=state_exemptions and a tabular JSONB flag where applicable"
      pattern: "state_exemptions"
    - from: "tests/test_source_onboarding.py"
      to: "src/brm/ingest/pdf.py"
      via: "tabular-flagged source routes through pdfplumber extraction"
      pattern: "tabular"
  decisions:
    - "D-12: State exemption sources reuse existing adapters — PDF rows use the Phase-3 PDF adapter, HTML statute rows point at the Phase-2 HTML adapter key as placeholders; no new adapter code is written."
    - "D-13: pdfplumber is added selectively — the California EJ-156 exemption-amounts PDF is flagged tabular=true so pdfplumber extracts its table; pypdfium2 stays the default for prose statute PDFs."
    - "D-14: Exemption scope is ingest + detect text changes only — a dollar-amount change inside a statute is detected as a text diff; parsing the amount into a structured value is deferred to Phase 5 (AI-04)."
    - "D-15: Exemption-statute Source-row granularity is determined per state at onboarding from the real statutes — onboarding stays a registry/config operation regardless of the row count (SRC-06)."
---

<objective>
Onboard the launch-state exemption sources — Oregon, California, and Texas — completing SRC-04. These reuse the existing PDF adapter with NO new adapter code (D-12): statute PDFs go through the PDF document adapter, and the California EJ-156 exemption-amounts PDF goes through the PDF adapter with the `tabular` flag set so pdfplumber extracts its table cleanly (D-13). Legislative HTML statute pages are seeded as placeholder rows pending the Phase-2 HTML adapter — see the scope note below.

Purpose: The v1 district tranche sits in three states; SRC-04 requires their exemption rules be monitored too. Phase 3's exemption scope is ingest + detect text changes only (D-14) — a dollar-amount change inside a statute is detected as a text diff; parsing the amount into a structured value is Phase 5 (AI-04), explicitly not pulled forward.
Output: launch-state exemption Source rows in `seed.py` and onboarding/tabular-extraction tests.

Scope note — HTML statute rows are placeholders (pattern_compliance, checker WARNING): D-12 says state HTML statute pages reuse "the Phase 2 HTML adapter". But `src/brm/ingest/html.py` does NOT exist yet — Phase 2 has planning artifacts only, no merged adapter code (confirmed in 03-PATTERNS.md "Codebase Note"). Therefore the OR ORS-18 page and the CA leginfo CCP §§703.140/704 pages are seeded as registry rows with `ingestion_method="html"` but are explicitly marked PLACEHOLDER / pending-Phase-2 in `seed.py` comments — they are valid registry entries (config-only onboarding, SRC-06) but their adapter does not exist until Phase 2 ships. The CA/TX PDF exemption sources (EJ-156 and Texas Property Code chapter PDFs) use the Phase-3 PDF adapter that this phase delivers — they are fully live. Task 2's test assertions cover only the PDF-adapter sources; they do NOT assert ingestion behavior for the HTML placeholder rows (no adapter to exercise). SRC-04 ("the source registry COVERS state exemption rules") is satisfied by the registry rows existing; live HTML ingestion of OR/CA statute pages activates when Phase 2's HTML adapter lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-CONTEXT.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-03-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Plans 03-01..03-03 built the adapters, schema, and district seed. -->

From src/brm/seed.py (extended by plan 03-03 — FRBP + 4 district rows already present):
- idempotent `_seed_in_session` pattern: match on `feed_url`, skip if exists, else construct `Source(...)`.

From src/brm/models/source.py — Source columns: jurisdiction, layer, feed_url, ingestion_method, adapter_ref, polling_cadence, health_status, source_pattern, tenant_id. A per-source JSONB config column carries the `tabular` flag (plan 03-01 schema; the PDF adapter reads it to decide pdfplumber routing).

From src/brm/ingest/pdf.py (plans 03-02/03-03) — `extract_pdf_text(pdf_bytes, tabular=...)`; the adapter determines `tabular` from the Source's per-source JSONB config. `run_pdf_ingest` is the PDF-source pipeline wrapper.

HTML statute pages: D-12 says reuse the Phase 2 HTML adapter — but `src/brm/ingest/html.py` does NOT exist yet (Phase 2 unimplemented; see 03-PATTERNS.md Codebase Note). HTML statute rows are seeded as PLACEHOLDER registry rows in this phase; no HTML ingestion is exercised here.

Launch-state exemption sources (RESEARCH Open Question 2 — verified/cited; re-verify URLs and Source-row count at onboarding):
- Oregon: ORS Chapter 18 exemptions — oregonlegislature.gov/bills_laws/ors/ors018.html — HTML adapter (placeholder pending Phase 2); ~1 Source row.
- California: CCP §§ 703.140 / 704.x — leginfo.legislature.ca.gov (confirmed static HTML) — HTML adapter (placeholder pending Phase 2); possibly several rows. PLUS the Judicial Council EJ-156 PDF (courts.ca.gov) — PDF document source, tabular=true — LIVE this phase.
- Texas: Property Code Ch. 41 / 42 — statutes.capitol.texas.gov per-chapter PDFs (PR.41.pdf, PR.42.pdf) — PDF adapter — LIVE this phase; ~2 rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboard launch-state exemption Source rows (OR, CA, TX)</name>
  <read_first>
    - src/brm/seed.py (the multi-row idempotent seed from plan 03-03 — extend it)
    - src/brm/models/source.py (Source columns; the per-source JSONB config column carrying the tabular flag)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (Open Question 2 — per-state ingestion method, Source-row granularity, the EJ-156 tabular PDF; Assumptions A3/A4/A6)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md (section: seed.py MODIFIED; Codebase Note — html.py does not exist yet)
    - .planning/phases/02-html-scraping-source-health/02-CONTEXT.md (per-source compliance record — robots.txt/ToS for .gov legislative sites)
  </read_first>
  <action>
  Extend `src/brm/seed.py` with launch-state exemption Source rows for Oregon, California, and Texas — the three states containing the v1 district tranche from plan 03-03. Each is an idempotent insert-if-not-exists matched on `feed_url`. NO new adapter code (D-12) — PDF rows point at the Phase-3 PDF adapter; HTML rows point at the Phase-2 HTML adapter key but are marked PLACEHOLDER (the HTML adapter is not yet built — 03-PATTERNS.md Codebase Note). Re-verify each statute URL is live at onboarding (RESEARCH Assumptions A3/A4); determine the exact Source-row count per state by inspecting the live pages per D-15 (RESEARCH Open Question 2 — one vs several rows; onboarding is config-only regardless per SRC-06).
  Rows to add — all `layer="state_exemptions"`, polling_cadence "daily", health_status "unknown", tenant_id None:
  - Oregon: jurisdiction "OR", ingestion_method "html", adapter_ref the HTML/scrape adapter key, feed_url = oregonlegislature.gov ORS Chapter 18 exemptions page. Likely one row. **Mark with a `# PLACEHOLDER — pending Phase 2 HTML adapter (src/brm/ingest/html.py not yet built)` comment** — the registry row is valid for SRC-04 coverage but live ingestion activates when Phase 2 ships.
  - California: jurisdiction "CA" —
    - HTML rows for the leginfo.legislature.ca.gov CCP §§ 703.140 / 704.x statute pages (one row per key section or per leginfo page — exact granularity decided per D-15 from the live pages), ingestion_method "html". **Mark each with the same `# PLACEHOLDER — pending Phase 2 HTML adapter` comment.**
    - PLUS one ingestion_method "pdf" row for the Judicial Council EJ-156 PDF (courts.ca.gov), source_pattern "document", adapter_ref "pdf_document", with the per-source JSONB config setting `tabular: true` so the PDF adapter routes it through pdfplumber (D-13 — EJ-156 carries the §703.140(b) dollar amounts as a table). This row is LIVE — the Phase-3 PDF adapter exists.
  - Texas: jurisdiction "TX", ingestion_method "pdf", source_pattern "document", adapter_ref "pdf_document" — two rows for Property Code Ch. 41 (PR.41.pdf) and Ch. 42 (PR.42.pdf) statute PDFs. These rows are LIVE — Phase-3 PDF adapter. (D-12 permits the HTML GetStatute.aspx view instead if the chapter PDFs prove unstable — but prefer the PDF chapter files since the HTML adapter is not yet available; if the PDFs are dead, seed an HTML placeholder row instead and note it.)
  Document the per-source compliance note (public unauthenticated `.gov` legislative sites, polite-scraping ceiling — Phase 2 D-03/D-08 carry-forward) in a comment. Keep every insert idempotent — running the seed twice must not duplicate rows.
  </action>
  <verify>
    <automated>uv run pytest tests/test_source_onboarding.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/brm/seed.py` adds `layer="state_exemptions"` Source rows for jurisdictions `OR`, `CA`, and `TX`
    - The California EJ-156 PDF row has its per-source JSONB config set with `tabular: true`
    - The OR and CA HTML statute rows carry an explicit `# PLACEHOLDER — pending Phase 2 HTML adapter` comment
    - At least one CA/TX row uses `ingestion_method="pdf"` and routes through the Phase-3 PDF adapter — proving adapter reuse, no new adapter
    - Running the seed twice produces no duplicate rows (idempotent insert-if-not-exists)
    - `uv run pytest tests/test_source_onboarding.py -q` passes — note this run proves only that the new seed rows insert idempotently; the tabular-extraction and exemption-text-change behaviors are proven by Task 2, whose `<verify>` is the full gate for those behaviors
  </acceptance_criteria>
  <done>The source registry covers exemption rules for all three launch states; CA/TX PDF rows reuse the live Phase-3 PDF adapter and OR/CA HTML rows are valid placeholder registry entries pending the Phase-2 HTML adapter — SRC-04 satisfied with no new adapter code. This task proves idempotent seeding only; tabular extraction and exemption-change detection are proven by Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Prove tabular extraction and exemption text-change detection</name>
  <read_first>
    - tests/test_source_onboarding.py (the onboarding tests from plan 03-03 — extend this file)
    - tests/test_pdf_adapter.py (the test_tabular_pdf test from plan 03-01 — the tabular fixture pattern)
    - src/brm/ingest/pdf.py (run_pdf_ingest; the adapter's tabular-flag routing)
    - src/brm/ingest/pdf_extract.py (extract_pdf_text tabular branch — plan 03-01)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (D-13, D-14; Assumption A6 — EJ-156 tabular structure)
  </read_first>
  <action>
  Add tests to `tests/test_source_onboarding.py` proving SRC-04's two load-bearing behaviors. These tests cover ONLY the PDF-adapter exemption sources (CA EJ-156, TX Property Code PDFs) — the OR/CA HTML statute rows are placeholders with no adapter yet (see this plan's scope note), so they are NOT exercised here. Reuse the `tabular.pdf` fixture from plan 03-01 (the EJ-156-style table PDF) — if a genuine EJ-156 PDF was captured for the seed row, additionally save it as `tests/fixtures/pdf/ej156.pdf` and use it.
  Test 1 — tabular extraction (D-13): construct a state-exemption PDF `Source` with the per-source JSONB config `tabular: true`; run the EJ-156-style PDF through `run_pdf_ingest` and assert the snapshot content is the stable row-rendered table text (a one-cell exemption-amount change would diff cleanly). Assert a non-tabular prose exemption PDF (config `tabular` unset) routes through pypdfium2 prose extraction, not pdfplumber.
  Test 2 — exemption text-change detection (D-14): replay an exemption statute PDF fixture pair where the SECOND version changes a dollar amount inside the statute text (e.g. a homestead exemption amount — use a Texas Property Code chapter PDF, which is a live PDF-adapter source). Run both through `run_pdf_ingest` and assert version 1 is a silent baseline (None) and version 2 produces a `detected` Change whose `diff_text` contains the changed amount. This proves Phase 3 detects the amount change AS A TEXT DIFF — explicitly NOT parsing the amount into a structured field (that is Phase 5 / AI-04, deferred). Capture or hand-construct the fixture pair into `tests/fixtures/pdf/`; record it in `SOURCES.md`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_source_onboarding.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_source_onboarding.py` contains a test asserting a `tabular: true` Source routes through pdfplumber and yields stable row-rendered table text
    - It contains a test asserting a non-tabular exemption PDF routes through pypdfium2 prose extraction
    - It contains a test replaying an exemption statute PDF pair where a dollar amount changes — version 1 is a baseline (None), version 2 is a `detected` Change whose `diff_text` includes the changed amount
    - The tests exercise only PDF-adapter sources; no assertion depends on the OR/CA HTML placeholder rows
    - No structured-amount parsing is asserted (D-14 — text diff only; structured extraction is deferred to Phase 5)
    - `uv run pytest -q` (full suite) passes
  </acceptance_criteria>
  <done>A tabular exemption PDF extracts cleanly via pdfplumber so a one-cell amount change diffs without reflow noise; an exemption statute text change — including a dollar amount — is detected as a text diff via the live PDF adapter, proving SRC-04's ingest+detect scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external legislative/court site → adapters | Untrusted statute/EJ-156 PDFs cross into the PDF adapter (HTML statute pages are placeholder rows — no adapter exercised this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | malformed statute/EJ-156 PDF crashing the parser | mitigate | Inherited — `extract_pdf_text` try/except (plan 03-01); pdfplumber extraction is also wrapped. No new parser surface in this plan |
| T-03-02 | Denial of Service | oversized statute PDF | mitigate | Inherited — `settings.pdf_max_bytes` size guard in the PDF adapter (plan 03-02) applies to every PDF source including state exemptions |
| T-03-10 | Spoofing / Tampering | a court PDF soft-404 read as "no exemption change" | mitigate | Inherited — a non-PDF or empty body extracts as near-empty → FETCH_FAILED, never UNCHANGED |
| T-03-11 | Tampering | state-exemption `feed_url` values | accept | `feed_url` values are operator-curated registry entries onboarded via the controlled config operation (SRC-06); arbitrary-URL monitoring is out of scope (REQUIREMENTS.md) |
</threat_model>

<verification>
- `uv run pytest tests/test_source_onboarding.py -q` passes.
- `uv run pytest -q` (full suite) passes — no regression.
- Running `uv run python -m brm.seed` twice produces no duplicate Source rows.
- No new adapter module is created — `git status` shows only `seed.py`, tests, and fixtures changed (in particular, no `src/brm/ingest/html.py` is created here — that is Phase 2).
</verification>

<success_criteria>
- The source registry covers exemption rules for Oregon, California, and Texas — the launch states of the v1 district tranche (SRC-04).
- State exemption PDF sources reuse the existing Phase-3 PDF adapter; OR/CA HTML statute rows are valid placeholder registry entries pending the Phase-2 HTML adapter; no new adapter code is written (D-12).
- The California EJ-156 PDF is flagged tabular so pdfplumber extracts its exemption-amount table cleanly (D-13).
- An exemption statute text change — including a dollar amount — is detected as a text diff via the live PDF adapter (D-14; structured extraction deferred to Phase 5).
- State exemption sources onboard as config-only registry rows (SRC-06).
</success_criteria>

<output>
Create `.planning/phases/03-pdf-ingestion-district-coverage/03-04-SUMMARY.md` when done.
</output>
