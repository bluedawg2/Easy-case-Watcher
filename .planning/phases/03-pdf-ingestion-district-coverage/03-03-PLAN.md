---
phase: 03-pdf-ingestion-district-coverage
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/brm/ingest/pdf.py
  - src/brm/models/change.py
  - src/brm/seed.py
  - tests/test_pdf_adapter.py
  - tests/test_pdf_fixture_replay.py
  - tests/test_source_onboarding.py
  - tests/fixtures/pdf/.gitkeep
autonomous: true
requirements: [INGEST-03, SRC-03]
user_setup: []

must_haves:
  truths:
    - "The PdfSourceAdapter handles index/listing-pattern sources: it parses an HTML order index and detects newly appeared order entries"
    - "A listing source's first poll records the existing index entries as a silent baseline — zero Change records"
    - "An order appearing after onboarding becomes a detected Change with a full-text addition diff"
    - "A document-pattern source diffs one canonical PDF; a listing-pattern source diffs the entry set — the two patterns are handled distinctly"
    - "The source registry holds ~3 federal bankruptcy court district sources covering local rules, standing orders, and general orders"
    - "A PDF-sourced Change carries the pdf_provenance flag recording it came from PDF extraction and whether OCR was involved"
    - "A real PDF-based district rule change flows end-to-end and produces a Change in the review-queue pipeline (criterion 5)"
  artifacts:
    - path: "src/brm/ingest/pdf.py"
      provides: "PdfSourceAdapter index/listing mode + source_pattern dispatch"
      contains: "index_listing"
    - path: "src/brm/seed.py"
      provides: "district Source rows — document + index_listing patterns across 3 districts"
      contains: "source_pattern"
    - path: "tests/test_pdf_fixture_replay.py"
      provides: "criterion-5 end-to-end PDF change replay test"
      contains: "run_ingest"
    - path: "tests/test_source_onboarding.py"
      provides: "config-only district onboarding proof (SRC-06)"
      contains: "def test"
  key_links:
    - from: "src/brm/ingest/pdf.py"
      to: "src/brm/detect/detector.py"
      via: "listing first-poll relies on the existing prior_snapshot-is-None baseline rule"
      pattern: "source_pattern"
    - from: "src/brm/seed.py"
      to: "src/brm/models/source.py"
      via: "district Source rows set source_pattern"
      pattern: "Source\\("
    - from: "tests/test_pdf_fixture_replay.py"
      to: "src/brm/pipeline.py"
      via: "criterion-5 replay through run_ingest"
      pattern: "run_ingest"
---

<objective>
Complete the district coverage vertical slice: add index/listing mode to `PdfSourceAdapter` (D-16/D-17/D-18), onboard the ~3-district tranche as config-only registry rows (SRC-03), stamp the `pdf_provenance` flag on PDF-sourced Changes (D-11), and prove criterion 5 — a real PDF-based district rule change flowing end-to-end into the review-queue pipeline.

Purpose: District courts publish two structurally different ways. Local rules are one canonical PDF (document pattern — already built in 03-02). General/standing orders are an HTML index of dated order PDFs (index/listing pattern) — a new order appears as a new index entry, not an edit to an existing document. Collapsing these into one detection rule is the explicit RESEARCH anti-pattern.
Output: index/listing mode in `pdf.py`, district seed rows, the `pdf_provenance` flag wired onto Changes, and the criterion-5 end-to-end replay test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-CONTEXT.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-01-SUMMARY.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Plans 03-01/03-02 built pdf_extract.py, pdf.py document mode, the schema. -->

From src/brm/ingest/pdf.py (plan 03-02 — document mode; this plan adds index_listing mode):
```python
async def fetch_source(source) -> FetchResult     # document mode; raises NotImplementedError for index_listing
async def _fetch_from_file(feed_file, source) -> FetchResult
class PdfSourceAdapter:
    def __init__(self, feed_file: str | None = None): ...
    async def fetch(self, source) -> FetchResult: ...
```

The HTML index parse should mirror the selectolax pattern in src/brm/ingest/rss.py `parse_entries` (lines 39-111): `HTMLParser(html)`, `tree.css(...)`, `node.text(strip=True)`, `a.attributes.get("href")`.

From src/brm/detect/detector.py — the first-fetch baseline rule that D-17 relies on (do NOT modify):
```python
if prior_snapshot is None:
    return None   # silent baseline — zero Changes
```

From src/brm/models/change.py (extended by plan 03-01) — pdf_provenance JSONB column:
```python
pdf_provenance: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
```

From src/brm/seed.py — the idempotent insert-if-not-exists pattern (`_seed_in_session`): match on `feed_url`, skip if exists, else construct a `Source(...)`. Source columns: jurisdiction, layer, feed_url, ingestion_method, adapter_ref, polling_cadence, health_status, source_pattern, tenant_id.

From src/brm/lifecycle.py — `STATUS_DETECTED = "detected"`.

District tranche (RESEARCH Open Question 1 — verified candidates; re-verify PDF URLs at onboarding):
- District of Oregon (orb.uscourts.gov/rules) — DOCUMENT pattern, local rules PDF; publishes a real prior-year + current LBR pair → the criterion-5 fixture pair.
- Central District of California (cacb.uscourts.gov/general-orders) — INDEX_LISTING pattern, general-orders dated table.
- Western District of Texas (txwb.uscourts.gov/standing-orders-index) — INDEX_LISTING pattern, standing-orders dated table.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add index/listing mode to PdfSourceAdapter + pdf_provenance stamping</name>
  <read_first>
    - src/brm/ingest/pdf.py (the document-mode adapter from plan 03-02 — the NotImplementedError extension point for index_listing)
    - src/brm/ingest/rss.py (parse_entries lines 39-111 — the selectolax HTML-index parse shape to mirror)
    - src/brm/detect/detector.py (the prior_snapshot-is-None baseline rule — D-17 relies on it; do not modify)
    - src/brm/pipeline.py (run_ingest — confirm it stays adapter-agnostic; do not modify)
    - src/brm/models/change.py (the pdf_provenance JSONB column from plan 03-01)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (Pattern 2 lines 221-234, Pitfall 5; D-16/D-17/D-18)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md (section: pdf.py — Index/listing mode; No Analog Found: index/listing set-difference)
  </read_first>
  <behavior>
    - Test: first poll of an index_listing source (no prior snapshot) → run_ingest returns None (silent baseline), one Snapshot stored holding the entry set, zero Change rows.
    - Test: second poll with one new order entry added to the index → a detected Change with a full-text addition diff for the new order.
    - Test: second poll with no new entries → UNCHANGED, no Change.
    - Test: a document-pattern source still behaves exactly as plan 03-02 built it (regression — the source_pattern dispatch did not break document mode).
    - Test: a Change produced by the PDF adapter has pdf_provenance == {"is_pdf": True, "ocr_involved": False} (a scanned source routed FETCH_FAILED, so ocr_involved is False for any Change that is actually produced in Phase 3).
  </behavior>
  <action>
  Extend `src/brm/ingest/pdf.py` with index/listing mode. Replace the `NotImplementedError` extension point from plan 03-02 with real dispatch on `source.source_pattern`: `"document"` or `None` → existing document-mode code; `"index_listing"` → new listing-mode code.
  Listing mode (D-16, RESEARCH Pattern 2):
  1. Fetch the HTML index page over httpx (reuse the conditional-GET fetch helper from plan 03-02 / `rss.py`).
  2. Parse order entries with selectolax — write a `parse_order_index(html: bytes) -> list[dict]` function mirroring `rss.py::parse_entries` shape; each entry is an identity dict (order number, title, date posted, PDF href).
  3. Render the parsed entry SET into a stable canonical string (sorted by a stable identity key — order number) and hash it. This string IS the snapshot content for a listing source. Return CHANGED with that content so `store_snapshot` records it and `detect_change` runs.
  4. D-17 silent-baseline: do NOT special-case the first poll in the adapter or in `pipeline.py`. The listing source's first poll stores the entry-set string as the first Snapshot; `detect_change`'s existing `prior_snapshot is None → return None` rule yields zero Changes automatically. The adapter just always returns CHANGED-with-entry-set on a successful fetch (the hash gate then collapses no-change polls to UNCHANGED).
  5. D-18 new-order detection: when the entry set changes, `detect_change` produces one Change whose `diff_text` is the unified diff of the entry-set strings — a brand-new order shows as an addition. (Phase 3 scope: the entry-set diff IS the change signal. Fetching each new order's individual PDF and re-diffing its body is a deeper enrichment; if the slice budget allows, additionally fetch the newest order PDF, extract+normalize it, and include its text in the Change — but the entry-set addition diff alone satisfies D-18's "full-text addition diff" for the listing pattern. Pick the entry-set-diff approach as the Phase-3 deliverable; do not leave a stub.)
  Also for SSRF defense (T-03-07): when listing mode resolves order-PDF hrefs, validate each resolved URL stays on the same host as the configured index `feed_url` before any fetch — reject off-host links.
  pdf_provenance stamping (D-11): `detect_change` creates the Change, but the `pdf_provenance` flag must be set for PDF-sourced Changes. Do this WITHOUT modifying `detect_change` or `pipeline.py` (they stay adapter-agnostic). Add it in `run_ingest`'s caller path is not available — instead, stamp it in the adapter is not possible either since the adapter does not create the Change. Resolve cleanly: after `run_ingest` returns a Change for a PDF source, the caller is responsible — but Phase 3 has no new caller. The correct seam: set `pdf_provenance` on the Change inside `detect_change` is forbidden. Therefore stamp it via a small post-step: expose a helper `stamp_pdf_provenance(change, ocr_involved=False)` in `pdf.py` and call it from the criterion-5 test path and the admin/seed flow; for the runtime pipeline, the cleanest non-pipeline-modifying option is to set `pdf_provenance` when the PDF adapter is the producer — implement this by having `run_ingest` already adapter-agnostic, so add the stamping in a thin wrapper function `run_pdf_ingest(session, source, adapter)` in `pdf.py` that calls `run_ingest` and, if a Change is returned, sets `change.pdf_provenance = {"is_pdf": True, "ocr_involved": False}` and flushes. Use `run_pdf_ingest` for PDF sources. Document that `run_ingest` itself stays untouched.
  Add tests to `tests/test_pdf_adapter.py`: `test_listing_first_poll_baseline`, `test_listing_new_order_addition`, `test_listing_no_new_entries_unchanged`, `test_document_mode_regression`, `test_pdf_provenance_flag`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pdf_adapter.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/brm/ingest/pdf.py` dispatches on `source.source_pattern` — `"index_listing"` runs listing mode, `"document"`/`None` runs document mode
    - `src/brm/ingest/pdf.py` defines `parse_order_index` and a `run_pdf_ingest` wrapper that stamps `pdf_provenance`
    - First poll of a listing source via `run_pdf_ingest` returns `None` and creates exactly one Snapshot and zero Change rows
    - A second poll with a new index entry returns a Change with `status == "detected"` and non-empty `diff_text`
    - A Change produced for a PDF source has `pdf_provenance == {"is_pdf": True, "ocr_involved": False}`
    - `git diff --stat src/brm/pipeline.py src/brm/detect/detector.py` shows no changes
    - `uv run pytest tests/test_pdf_adapter.py -q` passes
  </acceptance_criteria>
  <done>PdfSourceAdapter handles both district source patterns distinctly; a listing source's first poll is a silent baseline and a later new order is a detected addition; PDF-sourced Changes carry the provenance flag — all without modifying the pipeline or detector.</done>
</task>

<task type="auto">
  <name>Task 2: Onboard the ~3-district tranche as config-only registry rows</name>
  <read_first>
    - src/brm/seed.py (the idempotent `_seed_in_session` insert-if-not-exists pattern — match on feed_url)
    - src/brm/models/source.py (Source columns including the new source_pattern from plan 03-01)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (Open Question 1 — the three verified districts and their patterns; re-verify PDF/index URLs at onboarding)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md (section: seed.py MODIFIED)
    - .planning/phases/02-html-scraping-source-health/02-CONTEXT.md (the per-source compliance record — robots.txt/ToS — applies to court PDF sites)
  </read_first>
  <action>
  Extend `src/brm/seed.py` from a single FRBP row to a multi-row seed. Keep the existing FRBP insert; add district Source rows for the ~3-district tranche (RESEARCH Open Question 1). Each new row is an idempotent insert-if-not-exists matched on `feed_url`, mirroring `_seed_in_session`. Re-verify each PDF/index URL is live at onboarding time (court URLs drift — RESEARCH Pitfall 4); prefer a stable landing-page URL over a dated PDF filename where one exists.
  Rows to add (jurisdiction / layer / source_pattern / ingestion_method / adapter_ref):
  - District of Oregon local rules — jurisdiction "OR" (district of Oregon), layer "local_rules", source_pattern "document", ingestion_method "pdf", adapter_ref "pdf_document", feed_url = the orb.uscourts.gov local-rules PDF (or landing page).
  - Central District of California general orders — jurisdiction "CACB", layer "general_orders", source_pattern "index_listing", ingestion_method "pdf", adapter_ref "pdf_listing", feed_url = the cacb.uscourts.gov general-orders index page.
  - Western District of Texas standing orders — jurisdiction "TXWB", layer "standing_orders", source_pattern "index_listing", ingestion_method "pdf", adapter_ref "pdf_listing", feed_url = the txwb.uscourts.gov standing-orders index page.
  This covers SRC-03's required document types: local rules (document pattern), general orders + standing orders (listing pattern). polling_cadence "daily", health_status "unknown", tenant_id None on every row. Document the per-source compliance note (public unauthenticated `.uscourts.gov`, polite-scraping ceiling) in a comment per the Phase 2 D-03 carry-forward. Decision per D-04 (soft tie-breaker): the three districts span three states (OR, CA, TX), widening the SRC-04 launch-state proof — the PDF/activity criteria still drove selection per D-02/D-03.
  </action>
  <verify>
    <automated>uv run pytest tests/test_source_onboarding.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/brm/seed.py` inserts the FRBP row plus three district rows, each idempotent (matched on feed_url)
    - The seed includes exactly one `source_pattern="document"` district row (Oregon local rules) and two `source_pattern="index_listing"` district rows (CACB general orders, TXWB standing orders)
    - Running the seed twice produces no duplicate rows
    - The three district rows together cover layer values `local_rules`, `general_orders`, `standing_orders`
    - `uv run pytest tests/test_source_onboarding.py -q` passes
  </acceptance_criteria>
  <done>The source registry holds the ~3-district tranche covering local rules, general orders, and standing orders, onboarded as config-only idempotent registry rows — SRC-03 satisfied with no adapter code change per source.</done>
</task>

<task type="auto">
  <name>Task 3: Criterion-5 end-to-end PDF change replay + config-only onboarding proof</name>
  <read_first>
    - tests/test_fixture_replay.py (the end-to-end run_ingest replay test shape — lines 50-101)
    - tests/test_pdf_adapter.py (the PDF adapter tests from plans 03-01/03-02 and Task 1)
    - src/brm/seed.py (the district rows from Task 2)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-VALIDATION.md (Manual-Only Verifications — criterion 5; the planner may automate it as a fixture-replay test)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (Pattern 5 — the Oregon LBR superseded/replacement pair is the deterministic criterion-5 fixture; Open Question 1)
  </read_first>
  <action>
  Two test files proving the phase-closing criteria. Capture the criterion-5 fixture pair first: the District of Oregon LBR superseded-vs-replacement PDF pair (the prior-year LBR PDF and the current LBR PDF — both published per RESEARCH Open Question 1). Save them to `tests/fixtures/pdf/` as `oregon_lbr_prior.pdf` and `oregon_lbr_current.pdf`. If the prior-year version is no longer downloadable (RESEARCH Assumption A1), fall back to a hand-modified copy of the current PDF that still replays through the unchanged adapter (D-10's permitted alternative) — record which construction was used in `tests/fixtures/pdf/SOURCES.md`.
  `tests/test_pdf_fixture_replay.py` (criterion 5 / D-10): mirror `test_fixture_replay.py::test_fixture_replay_baseline_then_change`. Construct the Oregon LBR document Source with `db_session`; run `oregon_lbr_prior.pdf` through `run_pdf_ingest` with `PdfSourceAdapter` → assert `None` (silent baseline); run `oregon_lbr_current.pdf` → assert a `detected` Change with non-empty `diff_text` and `pdf_provenance["is_pdf"] is True`. This is the end-to-end demo: a real PDF-based district rule change becomes a Change in the review-queue pipeline. Assert the same fixture pair replayed via `_fetch_from_file` and via respx live-HTTP mock produce byte-identical `diff_text` (no production-only branch).
  `tests/test_source_onboarding.py` (SRC-06 config-only onboarding): with `db_session`, construct a brand-new district `Source` row in the test (no code change) for each pattern and assert `run_pdf_ingest` routes it correctly — a document-pattern row diffs a canonical PDF, an index_listing-pattern row diffs the entry set. This proves a new PDF source onboards as a pure registry operation. (This file is referenced by Task 2's verify; create it here and ensure Task 2's seed rows are also exercised — Task 2 and Task 3 both contribute tests to it; keep the file coherent.)
  Update `.planning/phases/03-pdf-ingestion-district-coverage/03-VALIDATION.md`: mark the criterion-5 manual-only row as automated by `tests/test_pdf_fixture_replay.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pdf_fixture_replay.py tests/test_source_onboarding.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/pdf/oregon_lbr_prior.pdf` and `oregon_lbr_current.pdf` exist as non-empty files; `SOURCES.md` records their origin and construction
    - `tests/test_pdf_fixture_replay.py` runs the Oregon LBR pair through `run_pdf_ingest` → asserts a baseline (None) then a `detected` Change with non-empty `diff_text`
    - The criterion-5 test asserts the produced Change has `pdf_provenance["is_pdf"] is True`
    - The criterion-5 test asserts `_fetch_from_file` and respx-live-HTTP replay of the same pair yield byte-identical `diff_text`
    - `tests/test_source_onboarding.py` proves both a document-pattern and an index_listing-pattern Source onboard and route with no code change
    - `03-VALIDATION.md` criterion-5 row is marked automated
    - `uv run pytest -q` (full suite) passes
  </acceptance_criteria>
  <done>A real Oregon LBR PDF rule change flows end-to-end through run_pdf_ingest into a detected Change with PDF provenance — criterion 5 met and automated; config-only onboarding proven for both district source patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external court site → PdfSourceAdapter | Untrusted HTML index pages and PDF binaries cross into the adapter |
| court index page → resolved order-PDF URLs | Hrefs parsed from an untrusted HTML index are followed to fetch order PDFs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | SSRF | listing-mode following order-PDF hrefs parsed from a court index page | mitigate | Validate every resolved order-PDF URL stays on the same host as the configured index `feed_url` before fetching; reject off-host links (Task 1) |
| T-03-08 | Denial of Service | a court index page lists hundreds of historical orders → first-poll flood | mitigate | D-17 silent-baseline: the first poll records the whole entry set as the baseline Snapshot, zero Changes — the existing `detect_change` first-fetch rule guarantees it (Task 1, RESEARCH Pitfall 5) |
| T-03-01 | Denial of Service | malformed order PDF crashing the parser | mitigate | `extract_pdf_text` try/except (plan 03-01) — inherited; listing mode reuses the same extraction path |
| T-03-09 | Tampering | `source_pattern` registry value drives a code path | accept | `source_pattern` is an operator-curated registry value constrained by the `ck_source_pattern` CHECK to `document`/`index_listing` only — not user input; onboarding is a controlled config operation (SRC-06) |
</threat_model>

<verification>
- `uv run pytest tests/test_pdf_adapter.py tests/test_pdf_fixture_replay.py tests/test_source_onboarding.py -q` passes.
- `uv run pytest -q` (full suite) passes — no FRBP regression.
- `pipeline.py` and `detector.py` are unmodified.
- Running `uv run python -m brm.seed` twice produces no duplicate Source rows.
</verification>

<success_criteria>
- `PdfSourceAdapter` handles document and index/listing patterns distinctly; a listing source's first poll is a silent baseline, a later new order is a detected addition.
- The source registry holds the ~3-district tranche covering local rules, general orders, and standing orders (SRC-03).
- PDF-sourced Changes carry the `pdf_provenance` flag (D-11).
- A real Oregon LBR PDF rule change flows end-to-end into a detected Change — criterion 5 met and automated.
- New PDF sources onboard as config-only registry rows (SRC-06).
</success_criteria>

<output>
Create `.planning/phases/03-pdf-ingestion-district-coverage/03-03-SUMMARY.md` when done.
</output>
</invoke>
