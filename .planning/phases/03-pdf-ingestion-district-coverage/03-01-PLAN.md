---
phase: 03-pdf-ingestion-district-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/brm/ingest/outcome.py
  - src/brm/ingest/pdf_extract.py
  - src/brm/config.py
  - src/brm/models/source.py
  - src/brm/models/change.py
  - alembic/versions/0002_pdf_district_columns.py
  - tests/conftest.py
  - tests/test_pdf_adapter.py
  - tests/fixtures/pdf/.gitkeep
autonomous: false
requirements: [INGEST-03]
user_setup: []

must_haves:
  truths:
    - "A clean text-layer PDF's bytes extract to non-empty normalized-ready text"
    - "A scanned/image-only PDF is classified image_only_pdf, not extraction_failed"
    - "A broken-text-layer PDF is classified extraction_failed, not image_only_pdf"
    - "A malformed PDF that crashes the parser is caught and classified extraction_failed, never crashing the worker"
    - "An oversized PDF above the size ceiling is rejected before parsing"
    - "A near-empty PDF produces a machine-readable triage reason code visible to source health monitoring (image_only_pdf vs extraction_failed)"
    - "Existing FRBP feed sources continue to produce CHANGED/UNCHANGED/FETCH_FAILED outcomes without disruption after the FetchResult change"
    - "source.source_pattern and change.pdf_provenance columns exist with VARCHAR+CHECK where string-typed"
  artifacts:
    - path: "src/brm/ingest/pdf_extract.py"
      provides: "pypdfium2/pdfplumber extraction + two-axis scanned-vs-broken classifier"
      contains: "def extract_pdf_text"
      min_lines: 80
    - path: "src/brm/ingest/outcome.py"
      provides: "FetchResult with optional reason_code field"
      contains: "reason_code"
    - path: "alembic/versions/0002_pdf_district_columns.py"
      provides: "additive migration: source.source_pattern + change.pdf_provenance"
      contains: "def upgrade"
    - path: "tests/test_pdf_adapter.py"
      provides: "fixture-replay test scaffold for every PDF extraction path"
      contains: "def test_text_layer_pdf_extracts"
  key_links:
    - from: "src/brm/ingest/pdf_extract.py"
      to: "pypdfium2"
      via: "import pypdfium2 as pdfium"
      pattern: "import pypdfium2"
    - from: "src/brm/ingest/pdf_extract.py"
      to: "src/brm/config.py"
      via: "near-empty thresholds read from Settings"
      pattern: "settings\\."
  decisions:
    - "D-05: Detect-and-escalate, not run-OCR — pdf_extract.py reliably detects image-only PDFs; the OCR engine itself stays a deferred stub this phase."
    - "D-06: A detected image-only PDF is recorded via the existing FETCH_FAILED tri-state outcome stamped with a reason code — FetchResult gains a reason_code field, no new outcome enum."
    - "D-07: The OCR seam is engine-agnostic — the image_only_pdf reason code is a generic activation signal with no engine preference recorded."
    - "D-08: The two-axis classifier distinguishes a scanned PDF (near-empty + image-dominated → image_only_pdf) from a broken text-layer PDF (near-empty + no large image objects → extraction_failed)."
    - "D-13: pdfplumber is added selectively — extract_pdf_text routes tabular=True PDFs through pdfplumber while pypdfium2 stays the default for prose."
---

<objective>
Build the PDF extraction core and the shared schema/dependency infrastructure that every later Phase-3 plan depends on. This is the foundation vertical slice: install pypdfium2/pdfplumber, add the `0002` migration for the district `source_pattern` column and the `change.pdf_provenance` flag, extend `FetchResult` with a machine-readable `reason_code`, and build `pdf_extract.py` — the genuinely novel two-axis scanned-vs-broken classifier (D-08) plus pypdfium2 prose extraction and selective pdfplumber table extraction (D-13).

Purpose: INGEST-03's load-bearing logic is reliably telling a scanned PDF (awaiting OCR — expected) apart from a broken text layer (a bug). Both extract as near-empty; the disambiguation is the image-area axis. Getting this wrong silently misclassifies court documents.
Output: A pure `extract_pdf_text()` function, the extended `FetchResult`, the `0002` migration, PDF config tunables, and the `tests/test_pdf_adapter.py` scaffold the next plans fill in.

Scope note: this plan has 4 tasks (2 lightweight checkpoints + 2 substantive auto/tdd tasks). The checker flagged this as borderline scope and concluded no split is required — the two checkpoints (package legitimacy gate, fixture capture) are lightweight verification/capture steps, not implementation work. Monitor context during execution; if Task 3 (the classifier) approaches 30% on its own, the executor should pause and flag rather than rush.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-CONTEXT.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md
@.planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Do NOT explore to rediscover these. -->

From src/brm/ingest/outcome.py — the dataclass to extend:
```python
class FetchOutcome(str, Enum):
    CHANGED = "changed"
    UNCHANGED = "unchanged"
    FETCH_FAILED = "fetch_failed"

@dataclass
class FetchResult:
    outcome: FetchOutcome
    content: str | None
    raw_etag: str | None
    raw_last_modified: str | None
    error: str | None
    # NEW field added by this plan: reason_code: str | None = None
```

From src/brm/config.py — the Settings singleton to extend (secrets have NO default; operational tunables SHOULD have defaults):
```python
class Settings(BaseSettings):
    database_url: str
    anthropic_api_key: str
    api_key: str
settings = Settings()
```

From src/brm/models/source.py — the VARCHAR+CHECK precedent for any new classifying column:
```python
__table_args__ = (
    CheckConstraint("health_status IN ('unknown', 'healthy', 'failed')", name="ck_source_health_status"),
)
ingestion_method: Mapped[str] = mapped_column(String, nullable=False)  # docstring already lists "pdf"
```

From src/brm/models/change.py — JSONB column precedent (summary):
```python
from sqlalchemy.dialects.postgresql import JSONB
summary: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
```

From alembic/versions/0001_initial_schema.py — migration metadata + hand-written discipline:
```python
revision = "0001"
down_revision = None
# autogenerate omits CheckConstraints — migrations are HAND-WRITTEN
```

pypdfium2 5.8.0 API primitives (RESEARCH Pattern 3):
- `pdf = pdfium.PdfDocument(pdf_bytes)` — accepts bytes
- `page.get_textpage()` → textpage; `textpage.count_chars()`, `textpage.get_text_bounded()`
- `page.get_size()` → (width, height); `page.get_objects()` → iterable of page objects
- image object: `obj.type == pdfium.raw.FPDF_PAGEOBJ_IMAGE`; `obj.get_pos()` → (l, b, r, t)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify and install pypdfium2 + pdfplumber (package legitimacy gate)</name>
  <read_first>
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (## Package Legitimacy Audit — slopcheck CLI was unconfirmed in the research session; both packages are CLAUDE.md-locked but the audit was done via `pip index versions`, not slopcheck)
    - CLAUDE.md (## Technology Stack — pypdfium2 default PDF extraction, pdfplumber selective)
    - pyproject.toml (current dependency list)
  </read_first>
  <action>
  Package legitimacy gate per the planner package-legitimacy protocol: slopcheck could not be confirmed in the research session, so pypdfium2 and pdfplumber are treated as requiring a human verification checkpoint before install. Both are named in CLAUDE.md's locked stack, so the risk is minimal — the human confirms registry provenance and approves. Verify pypdfium2 at https://pypi.org/project/pypdfium2/ (expect 5.8.0, Apache-2.0/BSD-3-Clause license, source repo github.com/pypdfium2-team/pypdfium2 — the official PDFium-team binding) and pdfplumber at https://pypi.org/project/pdfplumber/ (expect 0.11.9, source repo github.com/jsvine/pdfplumber). After approval, run `uv add pypdfium2 pdfplumber` then `uv sync`. Confirm both import: `uv run python -c "import pypdfium2; import pdfplumber; print(pypdfium2.__version__, pdfplumber.__version__)"`. PyMuPDF/fitz is FORBIDDEN by CLAUDE.md (AGPL) — do not install it under any circumstance.
  </action>
  <verify>
    <automated>uv run python -c "import pypdfium2; import pdfplumber; from pypdfium2.raw import FPDF_PAGEOBJ_IMAGE; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - https://pypi.org/project/pypdfium2/ shows version 5.8.0 with Apache-2.0/BSD-3-Clause license
    - https://pypi.org/project/pdfplumber/ shows version 0.11.9
    - `pyproject.toml` lists `pypdfium2` and `pdfplumber` as dependencies after `uv add`
    - `uv run python -c "import pypdfium2; import pdfplumber; from pypdfium2.raw import FPDF_PAGEOBJ_IMAGE; print('ok')"` prints `ok`
    - No `pymupdf` or `fitz` entry appears in `pyproject.toml`
  </acceptance_criteria>
  <what-built>An automated `uv add` of two PyPI packages named in CLAUDE.md's locked stack.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pypdfium2/ — confirm v5.8.0, Apache-2.0/BSD-3-Clause license, repo github.com/pypdfium2-team/pypdfium2.
    2. Open https://pypi.org/project/pdfplumber/ — confirm v0.11.9, repo github.com/jsvine/pdfplumber.
    3. Approve to proceed with `uv add pypdfium2 pdfplumber`.
  </how-to-verify>
  <resume-signal>Type "approved" to install, or describe a concern.</resume-signal>
  <done>Both packages installed, importable, FPDF_PAGEOBJ_IMAGE constant accessible; PyMuPDF absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FetchResult.reason_code, PDF config tunables, and the 0002 migration</name>
  <read_first>
    - src/brm/ingest/outcome.py (the FetchResult dataclass to extend additively)
    - src/brm/config.py (the Settings singleton to extend)
    - src/brm/models/source.py (VARCHAR+CHECK health_status precedent — lines 42-47)
    - src/brm/models/change.py (JSONB summary column precedent — lines 29, 98)
    - alembic/versions/0001_initial_schema.py (hand-written migration discipline — docstring lines 1-16, metadata lines 26-29)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md (sections: outcome.py MODIFIED, source.py MODIFIED, change.py MODIFIED, alembic 0002 NEW)
  </read_first>
  <behavior>
    - Test: a FetchResult constructed without reason_code defaults reason_code to None (back-compat with rss.py).
    - Test: a FetchResult constructed with reason_code="image_only_pdf" exposes that value.
    - Test: Settings exposes pdf_min_chars_per_page, pdf_image_ratio_threshold, pdf_max_bytes with non-None default values.
    - Test (DB): after `alembic upgrade head`, the source table has a source_pattern column and the change table has a pdf_provenance column.
    - Test (DB): inserting a Source with source_pattern="invalid_value" raises an IntegrityError (CHECK constraint enforced).
  </behavior>
  <action>
  Three additive changes plus one migration.
  (a) `src/brm/ingest/outcome.py`: add `reason_code: str | None = None` as the LAST field of `FetchResult` (must have a default so every existing `FetchResult(...)` call in `rss.py` keeps working). Document it in the dataclass docstring as the FETCH_FAILED triage code with vocabulary `image_only_pdf` | `extraction_failed`. Add module-level string constants `REASON_IMAGE_ONLY_PDF = "image_only_pdf"` and `REASON_EXTRACTION_FAILED = "extraction_failed"`.
  (b) `src/brm/config.py`: add three fields to `Settings` WITH defaults (operational knobs, not secrets): `pdf_min_chars_per_page: int = 100` (below this chars-per-page → near-empty), `pdf_image_ratio_threshold: float = 0.5` (image-area ratio above this → image-dominated page), `pdf_max_bytes: int = 50_000_000` (50 MB PDF size ceiling — RESEARCH Security Domain oversized-PDF DoS).
  (c) `src/brm/models/source.py`: add `source_pattern: Mapped[str | None] = mapped_column(String, nullable=True)` and a `CheckConstraint("source_pattern IN ('document', 'index_listing')", name="ck_source_pattern")` to `__table_args__`. Document the column. `ingestion_method` already documents "pdf" — leave it. Decision per D-16 discretion: use a dedicated `source_pattern` VARCHAR+CHECK column (not the freeform `layer` field, not implicit in `adapter_ref`) — it makes the document-vs-listing distinction a first-class queryable registry value per SRC-06, and VARCHAR+CHECK lets later phases add pattern values without `ALTER TYPE`. Do NOT add a separate `document_type` column — document type (local_rules / general_order / standing_order) is carried by the existing freeform `layer` field, keeping the schema change minimal.
  (d) `src/brm/models/change.py`: add `pdf_provenance: Mapped[dict | None] = mapped_column(JSONB, nullable=True)`. JSONB (not a bare boolean) because D-11 requires it to carry both "came from PDF extraction" and "whether OCR was involved" — shape `{"is_pdf": true, "ocr_involved": false}`. Nullable so non-PDF (FRBP) Changes leave it null. Document the column.
  (e) `alembic/versions/0002_pdf_district_columns.py`: hand-written (NOT autogenerated — restate in the docstring that autogenerate omits CheckConstraints). `revision = "0002"`, `down_revision = "0001"`, `branch_labels = None`, `depends_on = None`. `upgrade()`: `op.add_column("source", sa.Column("source_pattern", sa.String(), nullable=True))`, `op.create_check_constraint("ck_source_pattern", "source", "source_pattern IN ('document', 'index_listing')")`, `op.add_column("change", sa.Column("pdf_provenance", postgresql.JSONB(astext_type=sa.Text()), nullable=True))`. `downgrade()`: reverse order — `op.drop_constraint("ck_source_pattern", "source", type_="check")`, `op.drop_column("source", "source_pattern")`, `op.drop_column("change", "pdf_provenance")`.
  Run `uv run alembic upgrade head` against the dev DB and the brm_test DB so tests see the new columns.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run pytest tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `FetchResult(outcome=FetchOutcome.FETCH_FAILED, content=None, raw_etag=None, raw_last_modified=None, error="x")` constructs without error and `.reason_code is None`
    - `src/brm/ingest/outcome.py` defines `REASON_IMAGE_ONLY_PDF` and `REASON_EXTRACTION_FAILED` constants
    - `Settings` exposes `pdf_min_chars_per_page`, `pdf_image_ratio_threshold`, `pdf_max_bytes` and `settings` instantiates without setting new env vars
    - `alembic/versions/0002_pdf_district_columns.py` has `revision = "0002"` and `down_revision = "0001"`
    - `uv run alembic upgrade head` succeeds; `uv run alembic downgrade -1 && uv run alembic upgrade head` round-trips cleanly
    - After upgrade, `source` has a `source_pattern` column with a `ck_source_pattern` CHECK and `change` has a `pdf_provenance` JSONB column
    - `uv run pytest tests/test_models.py -q` passes
  </acceptance_criteria>
  <done>FetchResult extended back-compatibly; config tunables present with defaults; 0002 migration applies and reverses; source.source_pattern and change.pdf_provenance exist with the CHECK constraint enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build pdf_extract.py — extraction + two-axis scanned-vs-broken classifier</name>
  <read_first>
    - src/brm/detect/diff.py (the pure-transform module shape to mirror — docstring style, stdlib-only, no I/O)
    - src/brm/ingest/outcome.py (now carrying REASON_* constants from Task 2)
    - src/brm/config.py (the pdf_* tunables added in Task 2)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (Pattern 3 lines 236-262, Code Examples lines 346-369, Pitfalls 1/2, Security Domain threat table)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md (section: pdf_extract.py NEW — module docstring style, ExtractionResult dataclass shape)
    - tests/conftest.py (the mock_http / db_session fixtures and the Windows event-loop note)
  </read_first>
  <behavior>
    - Test: a clean text-layer PDF's bytes → ExtractionResult with text non-empty, chars_per_page above pdf_min_chars_per_page, is_near_empty False.
    - Test: a scanned/image-only PDF's bytes → is_near_empty True, image_ratio_high True (classifies as image_only_pdf).
    - Test: a broken-text-layer PDF (near-zero chars, no large image objects) → is_near_empty True, image_ratio_high False (classifies as extraction_failed).
    - Test: a malformed/truncated PDF byte string → extract_pdf_text raises no exception; returns an ExtractionResult flagged is_near_empty True with image_ratio_high False (extraction_failed-class), worker never crashes.
    - Test: passing tabular=True on a table-structured PDF routes through pdfplumber and returns a stable row-rendered text form.
  </behavior>
  <action>
  Create `src/brm/ingest/pdf_extract.py` as a pure module (no DB, no HTTP — accepts raw `bytes` only), mirroring `detect/diff.py`'s shape. Module docstring names RESEARCH Pattern 3, states "pure functions", states pypdfium2 is the default (Apache-2.0/BSD-3 — PyMuPDF FORBIDDEN by CLAUDE.md) and pdfplumber is selective (D-13).
  Define a frozen dataclass `ExtractionResult` with fields: `text: str`, `chars_per_page: float`, `image_ratio: float`, `is_near_empty: bool`, `image_ratio_high: bool`.
  Define `extract_pdf_text(pdf_bytes: bytes, tabular: bool = False) -> ExtractionResult`:
    1. Wrap the ENTIRE pypdfium2 parse in `try/except Exception` — RESEARCH Security Domain: a malformed/malicious PDF must NEVER crash the worker. On any exception return `ExtractionResult(text="", chars_per_page=0.0, image_ratio=0.0, is_near_empty=True, image_ratio_high=False)` (extraction_failed-class).
    2. `pdf = pdfium.PdfDocument(pdf_bytes)`. For each page: `count_chars()` from `get_textpage()` summed into total_chars; `get_size()` → page area summed; iterate `get_objects()`, for each object with `obj.type == FPDF_PAGEOBJ_IMAGE` add the `get_pos()` bounding-box area to image_area. Bound page iteration defensively (RESEARCH decompression-bomb mitigation) — iterate at most a configurable page ceiling, default 2000 pages.
    3. `chars_per_page = total_chars / page_count` (guard divide-by-zero); `image_ratio = image_area / page_area` (guard divide-by-zero → 0.0).
    4. Text body: if `tabular` is True, extract via pdfplumber (`pdfplumber.open(io.BytesIO(pdf_bytes))`, `page.extract_tables()`, render each `list[list[str|None]]` row to a stable pipe-joined text form so a one-cell change diffs cleanly — D-13); else join `textpage.get_text_bounded()` across pages with newlines.
    5. Two-axis classification (D-08): `is_near_empty = chars_per_page < settings.pdf_min_chars_per_page`; `image_ratio_high = image_ratio >= settings.pdf_image_ratio_threshold`. The caller (the adapter, plan 03-02) maps `is_near_empty AND image_ratio_high → image_only_pdf`, `is_near_empty AND NOT image_ratio_high → extraction_failed`.
  Thresholds come ONLY from `settings` — never hard-code them in this module.
  Create `tests/test_pdf_adapter.py` as the SCAFFOLD for all PDF tests (later plans add to it). Add a `FIXTURES_DIR = Path(__file__).parent / "fixtures" / "pdf"` constant and a `load_pdf(name) -> bytes` helper (mirror `test_fixture_replay.py` lines 25-31). Implement these tests now against `extract_pdf_text` directly: `test_text_layer_pdf_extracts`, `test_scanned_pdf_image_only`, `test_broken_text_layer`, `test_malformed_pdf_no_crash`, `test_tabular_pdf`. Add a `tests/fixtures/pdf/.gitkeep` so the directory exists; the actual PDF fixture bytes are captured in Task 4.
  Add a shared PDF fixture loader to `tests/conftest.py` (the Wave 0 VALIDATION requirement): a `load_pdf_fixture` helper or fixture that reads `tests/fixtures/pdf/<name>`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pdf_adapter.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/brm/ingest/pdf_extract.py` defines `ExtractionResult` and `def extract_pdf_text(pdf_bytes: bytes, tabular: bool = False) -> ExtractionResult`
    - `grep -v '^#' src/brm/ingest/pdf_extract.py | grep -c "settings\."` is at least 2 (thresholds read from config, not hard-coded)
    - `extract_pdf_text(b"%PDF-truncated-garbage")` returns an ExtractionResult with `is_near_empty=True` and does not raise
    - `tests/test_pdf_adapter.py` contains `test_text_layer_pdf_extracts`, `test_scanned_pdf_image_only`, `test_broken_text_layer`, `test_malformed_pdf_no_crash`, `test_tabular_pdf`
    - `tests/fixtures/pdf/.gitkeep` exists
    - `uv run pytest tests/test_pdf_adapter.py -q` passes (using fixtures captured in Task 4 — if Task 4 runs after, these tests may be skipped/xfail until fixtures land; the scaffold and `test_malformed_pdf_no_crash` which needs no fixture must pass)
  </acceptance_criteria>
  <done>extract_pdf_text extracts prose via pypdfium2, tables via pdfplumber when flagged, classifies near-empty PDFs on the two-axis rule with config-driven thresholds, and never crashes on malformed input.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Capture shared PDF fixtures (clean / scanned / broken / tabular)</name>
  <read_first>
    - tests/fixtures/ (existing frbp_source_v1.captured / frbp_source_v2.captured — the captured-bytes precedent)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-RESEARCH.md (Open Question 1 — empirically find a genuine scanned PDF; Validation Architecture Wave 0 Gaps)
    - .planning/phases/03-pdf-ingestion-district-coverage/03-PATTERNS.md (section: tests/fixtures/pdf/* NEW)
  </read_first>
  <action>
  Capture four real PDF fixture files into `tests/fixtures/pdf/`, each chosen to exercise one extraction path of `extract_pdf_text`:
  - `clean_text_layer.pdf` — a born-digital prose PDF with a real text layer (e.g. a District of Oregon local-rules PDF from orb.uscourts.gov/rules). Used by `test_text_layer_pdf_extracts`.
  - `scanned_image_only.pdf` — a genuine image-only/scanned PDF (RESEARCH Open Question 1: download 5-10 candidate signed general/standing order PDFs from cacb.uscourts.gov or txwb.uscourts.gov and run `extract_pdf_text` on each to find one that classifies image_only_pdf; if none of the three districts yields a real scan, re-scan a real court order to image-only PDF). Used by `test_scanned_pdf_image_only`.
  - `broken_text_layer.pdf` — a PDF with near-zero extractable chars and no large image objects (a deliberately corrupted text layer, or an empty/near-empty born-digital PDF). Used by `test_broken_text_layer`.
  - `tabular.pdf` — a genuinely table-structured PDF (the California Judicial Council EJ-156 exemption-amounts PDF, courts.ca.gov). Used by `test_tabular_pdf`.
  This is a `checkpoint:human-action` because capturing the genuine scanned PDF requires a human judgment call against live court sites (Open Question 1 explicitly defers it to execution) and may require re-scanning a document — there is no CLI for "find me a scanned PDF". The other three can be downloaded; capture all four in this step. Record the source URL of each fixture in a `tests/fixtures/pdf/SOURCES.md` note for provenance.
  </action>
  <verify>
    <automated>uv run python -c "from brm.ingest.pdf_extract import extract_pdf_text; from pathlib import Path; d=Path('tests/fixtures/pdf'); c=extract_pdf_text((d/'clean_text_layer.pdf').read_bytes()); s=extract_pdf_text((d/'scanned_image_only.pdf').read_bytes()); b=extract_pdf_text((d/'broken_text_layer.pdf').read_bytes()); assert not c.is_near_empty, 'clean must extract'; assert s.is_near_empty and s.image_ratio_high, 'scanned must be image_only'; assert b.is_near_empty and not b.image_ratio_high, 'broken must be extraction_failed'; print('fixtures ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/pdf/clean_text_layer.pdf`, `scanned_image_only.pdf`, `broken_text_layer.pdf`, `tabular.pdf` all exist as non-empty files
    - The verify command prints `fixtures ok` — proving clean extracts, scanned classifies image_only_pdf, broken classifies extraction_failed
    - `tests/fixtures/pdf/SOURCES.md` records the origin URL of each captured PDF
    - `uv run pytest tests/test_pdf_adapter.py -q` passes all five Task-3 tests with real fixtures
  </acceptance_criteria>
  <resume-signal>Type "captured" once all four PDF fixtures are saved and the verify command prints `fixtures ok`.</resume-signal>
  <done>Four PDF fixtures captured, each provably exercising its extraction path; the Task-3 test scaffold runs green against real bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external court site → PdfSourceAdapter | Untrusted PDF binaries downloaded from `.uscourts.gov` / `.gov` sites cross into the parser |
| PDF bytes → pypdfium2/pdfplumber | Untrusted file content reaches native-code (PDFium) and Python parsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | `pdf_extract.extract_pdf_text` parsing a malformed/malicious PDF | mitigate | Entire pypdfium2 parse wrapped in `try/except Exception`; on failure return an extraction_failed-class ExtractionResult — the worker never crashes (Task 3) |
| T-03-02 | Denial of Service | oversized PDF exhausting memory | mitigate | `settings.pdf_max_bytes` 50 MB ceiling; the adapter (plan 03-02) rejects oversized bytes before calling `extract_pdf_text` — the ceiling field is defined here in Task 2 |
| T-03-03 | Denial of Service | decompression bomb / deeply nested PDF (huge page count) | mitigate | Page-iteration bound (default 2000 pages) in `extract_pdf_text` as defense-in-depth on top of PDFium's internal limits (Task 3) |
| T-03-04 | Tampering / Info Disclosure | PDF with embedded JavaScript / external actions | accept | `get_textpage()` only reads text objects; pypdfium2 text extraction does not render or execute PDF actions. No mitigation code needed — extraction is read-only by construction |
| T-03-SC | Tampering | `uv add pypdfium2 pdfplumber` supply-chain | mitigate | Blocking human-verify checkpoint (Task 1) confirms PyPI provenance before install; both packages are CLAUDE.md-locked stack with long histories |
</threat_model>

<verification>
- `uv run alembic upgrade head` applies cleanly; `downgrade -1` then `upgrade head` round-trips.
- `uv run pytest tests/test_pdf_adapter.py tests/test_models.py -q` passes.
- `extract_pdf_text` on a truncated-garbage byte string returns an ExtractionResult without raising.
- `pyproject.toml` contains no `pymupdf`/`fitz` entry.
</verification>

<success_criteria>
- pypdfium2 5.8.0 and pdfplumber 0.11.9 installed and importable; PyMuPDF absent.
- `FetchResult` carries an optional `reason_code`; every existing `rss.py` construction still type-checks and runs.
- The `0002` migration adds `source.source_pattern` (VARCHAR+CHECK) and `change.pdf_provenance` (JSONB) and reverses cleanly.
- `extract_pdf_text` reliably distinguishes a scanned PDF (image_only_pdf) from a broken text layer (extraction_failed) on the two-axis rule, never crashes on malformed input, and reads thresholds from config.
- Four real PDF fixtures captured; the `tests/test_pdf_adapter.py` scaffold runs green.
</success_criteria>

<output>
Create `.planning/phases/03-pdf-ingestion-district-coverage/03-01-SUMMARY.md` when done.
</output>
